[project]
name = "mit-mot-experiment"
version = "0.1.0"
description = "Post analysis"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.0.0",
    "numpy<2.0.0",
    "opencv-python>=4.0.0",
]

[tool.uv]
package = false
